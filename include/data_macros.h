#ifndef DATA_MACROS_H
#define DATA_MACROS_H

#include "std_headers.h"

using data_type = char;
using data_index = unsigned short;
using data_size = unsigned int;
using str_type = std::string;
using char_type = char;

#endif